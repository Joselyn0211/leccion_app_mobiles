import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'leccion_joselyn_banchon',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
